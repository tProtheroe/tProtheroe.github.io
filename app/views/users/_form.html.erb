<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @user.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @user do |f| %>
  <%= f.label :username, 'Username:' %>
  <%= f.text_field :username, placeholder: 'Timmy Tammy' %>
  <%= f.label :email, 'Email:' %>
  <%= f.text_field :email, placeholder: 'tim@tam.com' %>
  <%= f.label :password, 'Password:' %>
  <%= f.text_field :password, placeholder: 'password12' %>
  <%= f.submit 'Submit' %>
<% end %>
